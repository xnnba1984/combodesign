% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{contribution_operating_characteristics}
\alias{contribution_operating_characteristics}
\title{Fixed-design operating characteristics for contribution contrasts}
\usage{
contribution_operating_characteristics(
  N,
  allocation,
  delta,
  rho = NULL,
  sigma2 = 1,
  alpha = 0.025,
  margin = NULL,
  claim_regime = c("full_contribution", "separate_component_claims"),
  adjustment_method = NULL,
  covariance_model = c("independent_arms", "general_correlated"),
  arms = c("A", "B", "AB"),
  contribution_map = NULL,
  integer_allocation = TRUE
)
}
\arguments{
\item{N}{Total sample size.}

\item{allocation}{Named allocation proportions for the trial arms.}

\item{delta}{Named standardized contribution effects. For the default
three-arm setting, use names \code{AB_minus_A} and \code{AB_minus_B}.}

\item{rho}{Optional arm-level correlation matrix with row and column names
matching \code{arms}. This is allowed only when \code{covariance_model} is
\code{"general_correlated"}.}

\item{sigma2}{Common endpoint variance.}

\item{alpha}{One-sided type I error rate. The default is \code{0.025}.}

\item{margin}{Optional contribution margins. Use names \code{AB_minus_A} and
\code{AB_minus_B} in the default setting. The default is zero margins.}

\item{claim_regime}{Either \code{"full_contribution"} for the primary
intersection union rule or \code{"separate_component_claims"} for optional
separately reportable component claims.}

\item{adjustment_method}{Optional adjustment method. Defaults to \code{"iut"} for
full contribution and \code{"maxT"} for separate component claims.}

\item{covariance_model}{\code{"independent_arms"} for the primary randomized
clinical trial model or \code{"general_correlated"} for a justified
correlated-estimator extension.}

\item{arms}{Character vector of arm names.}

\item{contribution_map}{Optional data frame with columns \code{combination},
\code{base}, and \code{component}.}

\item{integer_allocation}{Whether deterministic integer arm sizes should be
used for the fixed total sample size.}
}
\value{
A list containing the contribution contrast matrix, contrast
covariance, one-sided critical value, joint power, marginal
powers, allocation, and sample sizes.
}
\description{
Computes operating characteristics for a fixed total sample size and
allocation. The default claim regime is full conjunctive contribution, which
uses intersection union testing. The optional separate component-claim
regime uses maxT by default. The default arm labels are \code{A}, \code{B}, and \code{AB},
with contribution contrasts \code{AB_minus_A} and \code{AB_minus_B}.
}
\examples{
delta <- c(AB_minus_A = 0.45, AB_minus_B = 0.55)
allocation <- c(A = 1/3, B = 1/3, AB = 1/3)
contribution_operating_characteristics(
  N = 120,
  allocation = allocation,
  delta = delta
)
}

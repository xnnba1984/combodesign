% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{optimal_design}
\alias{optimal_design}
\title{Optimal design wrapper for the contribution framework}
\usage{
optimal_design(
  delta,
  rho = NULL,
  sigma2 = 1,
  target_power = 0.8,
  alpha = 0.025,
  margin = NULL,
  claim_regime = c("full_contribution", "separate_component_claims"),
  adjustment_method = NULL,
  covariance_model = c("independent_arms", "general_correlated"),
  arms = c("A", "B", "AB"),
  contribution_map = NULL,
  min_allocation = 0.02,
  N_upper = 1e+06
)
}
\arguments{
\item{delta}{Named standardized contribution effects. For the default
three-arm setting, use names \code{AB_minus_A} and \code{AB_minus_B}.}

\item{rho}{Optional arm-level correlation matrix with row and column names
matching \code{arms}. This is allowed only when \code{covariance_model} is
\code{"general_correlated"}.}

\item{sigma2}{Common endpoint variance.}

\item{target_power}{Target joint power.}

\item{alpha}{One-sided type I error rate. The default is \code{0.025}.}

\item{margin}{Optional contribution margins. The default is zero margins.}

\item{claim_regime}{Either \code{"full_contribution"} or
\code{"separate_component_claims"}.}

\item{adjustment_method}{Optional adjustment method. Defaults to \code{"iut"} for
full contribution and \code{"maxT"} for separate component claims.}

\item{covariance_model}{Primary \code{"independent_arms"} model or
\code{"general_correlated"} extension.}

\item{arms}{Character vector of arm names.}

\item{contribution_map}{Optional data frame with columns \code{combination},
\code{base}, and \code{component}.}

\item{min_allocation}{Minimum allowed arm allocation proportion.}

\item{N_upper}{Upper bound used by the sample-size search.}
}
\value{
A list with a comparison summary and fitted design objects.
}
\description{
This function is retained as a compatibility name for earlier package users.
It calls \code{\link[=component_contribution_design]{component_contribution_design()}} and uses one-sided \code{AB_minus_A}
and \code{AB_minus_B} contribution effects. Older argument sets are not supported
by this wrapper.
}
\examples{
delta <- c(AB_minus_A = 0.55, AB_minus_B = 0.65)
optimal_design(delta = delta, target_power = 0.70, min_allocation = 0.10)
}
